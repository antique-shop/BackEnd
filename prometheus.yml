global:
  scrape_interval: 5s  # 5초마다 데이터를 수집

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']  # Spring Boot 서버의 주소
